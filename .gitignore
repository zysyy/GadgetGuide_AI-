# 1. 敏感的环境变量文件
.env

# 2. macOS 操作系统自动生成的隐藏文件和目录
# ----------------------------------------------------
# Finder 文件夹自定义视图和元数据
.DS_Store

# AppleDouble 文件格式，用于在非 HFS+ 文件系统上存储 Mac 特有的元数据
._*

# Spotlight 索引文件 (通常在卷的根目录，但有时可能在项目内)
.Spotlight-V100

# 挂载卷上的本地回收站
.Trashes

# 某些应用的临时文件或备份文件，有时会以 . 开头或 ~ 结尾
# *~
# ._*



# 3. Python 相关的忽略项 (您可能已经有了或可以考虑添加)
# ----------------------------------------------------
__pycache__/
*.py[cod]
*.so
*.egg-info/
dist/
build/
*.egg
*.log
# Conda 环境 (如果在项目目录内创建了环境，并且不想提交)
# env/
# venv/

# 4. FAISS 索引和上传的文件 (根据项目结构)
# ----------------------------------------------------
# 假设 .gitignore 在 backend 目录下，或者在项目根目录并指定路径
faiss_index/
uploads/
# 如果 .gitignore 在项目根目录，并且这些文件夹在 backend 下：
# backend/faiss_index/
# backend/uploads/